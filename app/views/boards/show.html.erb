<div class="container mt-4">
  <div class="board-details">
    <h1 class="board-name"><%= @board.name %></h1>
    <p class="generated-by">Generated by <%= @board.email %></p>
  </div>

  <h2 class="section-title">Minesweeper Board</h2>
  <div class="row justify-content-center">
    <div class="minesweeper-board">
      <% @board.data.each do |row| %>
        <div class="row">
          <% row.each do |cell| %>
            <div class="col cell <%= cell == 'M' ? 'mine' : 'empty' %>"><%= cell == 'M' ? '💣' : '⬜' %></div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
